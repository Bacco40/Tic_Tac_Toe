const game = ((name1,name2,simbol1,simbol2)=>{
    const gameboard={};
    const player=(name,simbol)=>{
        this.name=name;
        this.simbol=simbol;
    }
    const player1=new player(name1,simbol1);
    const player2=new player(name2,simbol2);
})();